[project]
name = "claude-architect-course"
version = "0.1.0"
description = "Exercises for the Claude architect course"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lib"]
